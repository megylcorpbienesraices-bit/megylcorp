# Three.js adapter
Optional source-level adapter for teams that standardize on Three.js. The packaged
v1.24 runtime uses raw WebGPU first and custom WebGL/GLSL as fallback, avoiding a large
framework dependency while preserving the same shader-driven terrain architecture.
